# ☁️ CloudConvert API Frontend Project

[![](https://img.shields.io/badge/-Перейти%20на%20Сайт-337EFF?style=for-the-badge)](https://zebro-pentest.github.io/CloudConvert_project_API/CloudConvert_API/index.html)

> Простой проект для практики работы с API. Позволяет загружать файлы и конвертировать их в другие форматы через CloudConvert. Проект на стадии разработки и предназначен для обучения работе с внешними API и созданию базового интерфейса. Также для удобства используйте devtools нажав на кнопку F12 открыв раздел 'консоль'<br>

---

## 🚀 Описание проекта

Это простой веб-проект, созданный для **практики работы с внешними API**. Он демонстрирует базовую интеграцию с **CloudConvert API**, позволяя пользователю загружать файлы и конвертировать их в различные форматы через интуитивный интерфейс.

Проект сосредоточен на освоении логики **Job → Tasks** (Upload → Convert → Export) и создании базовой фронтенд-оболочки для работы с асинхронными процессами конвертации.

> **Демонстрационная версия:** На сайте используется демо-режим JavaScript для наглядной иллюстрации работы интерфейса и логики API. Для полноценного использования с реальными файлами требуется настройка, описанная ниже.

---

## 🛠️ Начало работы (Getting Started)

Для полноценного использования проекта с вашими файлами необходимо выполнить следующую настройку:

### Шаг 1: Получение проекта
1.  **Клонируйте проект (Download/Clone):**
    * **Через Git:** Используйте команду `git clone https://github.com/ZeBro-pentest/CloudConvert_project_API.git`.
    * **Через ZIP:** На странице репозитория нажмите **Code** и выберите **Download ZIP**.

2.  **Запуск локально:**
    * Откройте файл `index.html` в вашем веб-браузере для локального тестирования.
### Шаг 2: Получение API-ключа
1.  **Регистрация:** Зарегистрируйтесь на [cloudconvert.com](https://cloudconvert.com/).
2.  **Переход в настройки:** После входа в систему перейдите в раздел **Developers** (Разработчики).
3.  **Получение ключа:** В разделе **API Keys** сгенерируйте и скопируйте ваш **персональный API-ключ**.

    > ⚠️ **Внимание:** Ключ показываеться только 1 раз, также не делитесь этим ключом публично, так как он дает доступ к вашему аккаунту и оплаченным лимитам.
### Шаг 3: Интеграция ключа в проект
Ключ необходим для авторизации всех запросов к API.
1.  Откройте файл `script.js` в текстовом редакторе.
2.  **Найдите переменную:** Найдите в коде место, где определяется переменная для ключа, например:
    ```javascript
    const apiKey = "apiKey"; 
    ```
3.  **Вставьте ключ:** Замените заглушку (`apiKey`) на ваш скопированный ключ. <br>
⚠️ ️ также замените путь к скрипту в файле `index.html`, по умолчанию стоит путь `DEMO.js`
### Шаг 4: Мониторинг активности API

Для отслеживания лимитов и статуса конвертаций используйте следующие разделы на сайте CloudConvert:

| Раздел | Назначение |
| :--- | :--- |
| **Dashboard** (Панель) | Отслеживание использования **ежедневного лимита** (25 минут), баланса и тарифного плана. |
| **API Keys** (Ключи) | Управление вашими ключами, их генерация и отзыв. |
| **Jobs** (Задания) | **Просмотр статусов** всех конвертаций (отправленных заданий). Здесь вы видите, ждет ли задание в очереди, выполняется ли, или завершилось ошибкой. |

Теперь ваш локальный проект готов к работе с реальным CloudConvert API!

## 🔑 Об API и Ограничениях

Если вы используете **бесплатный (Free) план** CloudConvert API. При работе с ним учитывайте следующие ограничения:

| Категория | Ограничение бесплатного плана |
| :--- | :--- |
| **Параллельные задания** | ❌ Не более **1 задания** одновременно. Попытка запустить второе задание вызовет ошибку "Too many jobs". |
| **Лимит времени** | ❌ **25 минут** конвертации в день. Лимит обновляется на следующий день. |
| **Хранение файлов** | ❌ Файлы хранятся только **24 часа** после конвертации, затем автоматически удаляются. |
| **Приоритет** | ❌ Низкий приоритет. Ваши задания могут ожидать в очереди. |

---

## 💡 Основная логика работы (Job Flow)

CloudConvert API работает на фронтенде по структуре **Job → Tasks**. Каждое задание (Job) состоит из трех обязательных последовательных этапов (Tasks):

1.  **Import Task** (Загрузка): Файл загружается в систему CloudConvert (через `upload` или `URL`).
2.  **Convert Task** (Конвертация): Указывается, какой импортированный файл и в какой формат необходимо конвертировать.
3.  **Export Task** (Экспорт): Получение ссылки для скачивания готового, конвертированного файла.

**Схема работы:**
$$\text{Upload} \to \text{Convert} \to \text{Export}$$
